export enum Services {
  Auth = 'AUTH_SERVICE',
  Users = 'USERS_SERVICE',
  Sessions = 'SESSIONS_SERVICE',
  Scans = 'SCANS_SERVICE',
  RabbitMq = 'RABBITMQ_SERVICE',
}

export enum Params {
  ScanId = 'scanId',
}

export enum Controllers {
  Scans = 'scans',
  Auth = 'auth',
  App = '',
}
